doctype
html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    title An Angular App
    meta(name='description', content='')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel='stylesheet', href='dist/css/ngsandbox.min.css')
  body(ng-app="codeschool")

    #main.row
        //- creates a scope!
        .large-12.columns(ng-controller="CSController as cs")
            .large-4.columns(ng-repeat="product in cs.products")
                h1  {{product.name}}
                h2  ${{product.price}}
                p {{product.description}}
                button(ng-show="product.canPurchase") Add to Cart


        - var swear = 'You prick! :('
        - var anotherSwear = 'Test test test!'

        .row
            .large-6.large-centered.columns
                p It's another attempt at making it work! Please work #{swear}
                p It's another what? Of course, #{swear}


    script(src="dist/js/ngsandbox.min.js")
